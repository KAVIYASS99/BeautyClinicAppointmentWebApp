apiVersion: v1
kind: Service
metadata:
  name: appointment-service
spec:
  type: NodePort
  selector:
    app: appointment-service
  ports:
    - protocol: TCP
      port: 8082       # internal port
      targetPort: 8082 # container port
      nodePort: 30082  # accessible port on your localhost
